describe('Create Category', () => {
  it('Result should be equal a 4', () => {
    const sum = 2 + 2;
    const result = 4;

    expect(sum).toBe(result);
  });

  it('Result should not be equal a 4', () => {
    const sum = 2 + 2;
    const result = 5;

    expect(sum).not.toBe(result);
  });
});
